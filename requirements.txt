sf-toolkit>=0.5
# httpx is also a transitive dep of sf-toolkit (SalesforceClient subclasses
# httpx.Client). We list it explicitly because we use it directly for the
# Attune keystore round-trips and for any non-JSON Salesforce calls (CSV
# upload/download, CometD long-poll) that go through the toolkit's client.
httpx>=0.26
PyJWT[crypto]>=2.8
cryptography>=42
